# Prevent hoisting issues between main project and Documents/disco-agent-saas
legacy-peer-deps=false
save-exact=false

# Use local node_modules for each project
prefer-local=true

# Prevent automatic package-lock updates during install
package-lock=true
